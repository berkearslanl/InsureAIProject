@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sayfa Bulunamadı | Sigorta Güvencesi</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #0052cc; /* Klasik güven veren sigorta mavisi */
            --secondary-color: #f4f7fa;
            --text-color: #1d2b41;
            --accent-color: #ff5a5f;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--secondary-color);
            color: var(--text-color);
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
            overflow: hidden;
        }

        .container {
            max-width: 600px;
            padding: 40px;
            background: #ffffff;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.05);
            position: relative;
            z-index: 1;
        }

        /* Arkaplan dekoru için */
        .bg-pattern {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            opacity: 0.5;
            pointer-events: none;
        }

        h1 {
            font-size: 120px;
            font-weight: 800;
            line-height: 1;
            color: var(--primary-color);
            margin-bottom: 10px;
            letter-spacing: -5px;
        }

        h2 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        p {
            font-size: 16px;
            line-height: 1.6;
            color: #6c757d;
            margin-bottom: 30px;
        }

        .btn {
            display: inline-block;
            background-color: var(--primary-color);
            color: #fff;
            padding: 15px 35px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 10px 20px rgba(0, 82, 204, 0.2);
        }

            .btn:hover {
                transform: translateY(-3px);
                box-shadow: 0 15px 25px rgba(0, 82, 204, 0.3);
                background-color: #003d99;
            }

        .icon-box {
            font-size: 60px;
            margin-bottom: 20px;
        }

        /* Küçük bir animasyon */
        keyframes float {
            0%

        {
            transform: translateY(0px);
        }

        50% {
            transform: translateY(-10px);
        }

        100% {
            transform: translateY(0px);
        }



        .floating {
            animation: float 4s ease-in-out infinite;
        }

        }
    </style>
</head>
<body>

    <div class="bg-pattern">
    </div>

    <div class="container">
        <div class="icon-box floating">🛡️</div>
        <h1>404</h1>
        <h2>Beklenmedik Bir Durumla Karşılaştık</h2>
        <p>
            Aradığınız sayfa poliçe kapsamımız dışında kalmış veya taşınmış olabilir.
            Endişelenmeyin, dijital asistanlarımız sizi güvenli bölgeye ulaştırmak için burada.
        </p>
        <a href="/Default/Index/" class="btn">Ana Sayfaya Dön</a>
    </div>

</body>
</html>